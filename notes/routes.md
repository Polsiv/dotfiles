# Important routes

### /etc/passwd
- Stores users and their info

### /etc/group
- Stores the groups info

### /etc/shadow
- Stores the encrypted passwords2

### /etc/fstab
- Stores the partition information

### /etc/network/interfaces
- Stores the config for the network interfaces

### /etc/hostname
- Stores the hostname 
